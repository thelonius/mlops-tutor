# Secrets — НИКОГДА не коммитить .env
.env
.env.local
*.key

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.venv/
venv/
env/

# IDE
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# Logs
*.log

# Flask
instance/
.webassets-cache

# SQLite-файлы Flask приложения (хранилище shares). data/tts_terms.tsv
# нужен в репе — добавляем явным `git add`. Здесь блокируем только БД.
data/*.db
data/*.db-shm
data/*.db-wal

# Frontend
frontend/node_modules/
frontend/dist/
frontend/*.tsbuildinfo
# Vite build output для прода. На CI собирается заново перед scp.
static/dist/

# Claude Code local worktrees (не коммитим как gitlinks)
.claude/worktrees/
